<div class="login-page">
  <%= form_for(resource, as: resource_name, url: session_path(resource_name), :html => {:class => 'envbooking'}) do |f| %>

    <h3>Log in</h3>

    <div class="control-group">
      <%= f.label :email, :class => 'control-label' %>
      <div class="controls">
        <%= f.email_field :email, autofocus: true, :tabindex => 1, :class => 'form-control' %>
      </div>
      <%= error_span(@user[:email]) %>
    </div>

    <div class="control-group">
      <%= f.label :password, :class => 'control-label' %><br />
      <div class="controls">
        <%= f.password_field :password, autocomplete: "off", :tabindex => 2, :class => 'form-control' %>
      </div>
      <%= error_span(@user[:password]) %>
    </div>
    <br/>

    <div class="control-group">
      <div class="actions">
        <%= f.submit "Log in", :class => 'btn btn-primary' %>
      </div>
    </div>

    <%= render "devise/shared/links" %>

    <br/>
    <br/>

  <% end %>
</div>
