<!DOCTYPE HTML>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Application to book test environments">
    <meta name="author" content="JRG">

    <title><%= content_for?(:title) ? yield(:title) : "Envbooker" %></title>
    <%= csrf_meta_tags %>

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js"></script>
        <script src="https://oss.maxcdn.com/libs/respond.js/1.4.2/respond.min.js"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
    <%= javascript_include_tag "application" %>

    <%= yield :visualisation_library %>
    <%= yield :vis_entry_point %>

    <script type="text/javascript">
      <%= yield :script_entry_point %>
    </script>

    <%= yield :extra_head_stuff %>

  </head>
  <body>

    <% unless params[:controller].include?('devise') || params[:controller].include?('registrations') %>
      <%= render :partial => 'home/navbar' %>
    <% end %>

    <div class="container-fluid">
      <div class="row">
        <div class="col-md-6 col-md-offset-3">
          <div class="alert alert-success" style="display: none;"></div>
          <div class="alert alert-danger" style="display: none;"></div>
          <div class="alert alert-warning" style="display: none;"></div>
          <div class="alert alert-info" style="display: none;"></div>
        </div>

        <div class="col-md-6 col-md-ofset-3" id="banner-alerts">
          <%= bootstrap_flash %>
        </div>
      </div>

      <div class="row">
        <div class="col-lg-12">
          <%= yield %>
        </div>
      </div>


      <div class="container-fluid">
        <div class="row-fluid">
          <div class="col-lg-12">
            <%= yield :timeline_divs %>
          </div>
        </div>
      </div>
      <footer>
      </footer>
    </div>
  </body>
</html>
