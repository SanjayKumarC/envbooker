<%= form_for(@envapp) do |f| %>
  <% if @envapp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@envapp.errors.count, "error") %> prohibited this envapp from being saved:</h2>

      <ul>
      <% @envapp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :env_id %><br>
    <%= f.text_field :env_id %>
  </div>
  <div class="field">
    <%= f.label :app_id %><br>
    <%= f.text_field :app_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
